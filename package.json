{
  "scripts": {
    "start": "npm run bundle-install && npm run bundle-build && npm run generate-previews && npm run bundle-exec",
    "generate-previews": "node .github/workflows/generate_previews.js",
    "bundle-install": "bundle install --local",
    "bundle-build": "bundle exec jekyll build",
    "bundle-exec": "bundle exec jekyll serve --verbose"
  },
  "dependencies": {
    "puppeteer": "^24.0.0"
  }
}
